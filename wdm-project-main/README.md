# wdm-project
## Frontend use "npm start" to run for first time use install dependencies npm install npm update and if you find any dependencies missing just install them using " npm install ..."
## Backend use "npm run dev" for first time use install these dependencies npm install cors npm install axios and if you find any dependencies missing just install them using " npm install ..."
